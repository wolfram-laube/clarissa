# Architecture diagrams

Mermaid sources live in `docs/architecture/diagrams/*.mmd`.

CI job `architecture_graphs` attempts to render these into SVGs under `docs/architecture/rendered/`.
If your environment lacks Mermaid rendering support, the `.mmd` sources remain the canonical truth.
