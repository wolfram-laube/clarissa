# 🦊 CLARISSA GitLab Workflow - ورقة مرجعية

<div dir="rtl">

> **باختصار:** Issue ← Branch ← Commit ← MR ← Merge ← الـ Issue يُغلق تلقائياً

---

## 🔄 سير العمل في ٥ خطوات

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  ٥. مراجعة  │←───│  ٤. إنشاء   │←───│ ٣. Commit  │←───│  ٢. إنشاء   │←───│  ١. اختيار  │
│   ودمج     │    │    MR      │    │   & Push   │    │   Branch   │    │   Issue    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

---

## ١️⃣ اختيار Issue من اللوحة

```
Project → Plan → Issue Boards
```

- خذ issue من عمود **Ready**
- انقله إلى **In Progress**

---

## ٢️⃣ إنشاء Branch

```bash
# ابدأ دائماً برقم الـ issue!
git checkout -b 42-short-description
```

| ✅ صحيح | ❌ خطأ |
|---------|--------|
| `42-add-login` | `my-branch` |
| `12-fix-bug` | `test123` |
| `7-update-docs` | `changes` |

---

## ٣️⃣ Commit مع إشارة للـ Issue

```bash
git commit -m "type: الوصف #42"
```

### بادئات Conventional Commits:

| البادئة | الاستخدام |
|---------|-----------|
| `feat:` | ميزة جديدة |
| `fix:` | إصلاح خطأ |
| `docs:` | توثيق |
| `chore:` | صيانة |
| `refactor:` | إعادة هيكلة |
| `test:` | اختبارات |

### أمثلة:

```bash
git commit -m "feat: إضافة مصادقة المستخدم #42"
git commit -m "fix: إصلاح null pointer #15"
git commit -m "docs: تحديث الدليل #7"
```

---

## ٤️⃣ إنشاء Merge Request

```bash
git push -u origin 42-short-description
```

### قالب وصف MR:

```markdown
## ملخص
وصف قصير للتغييرات.

## التغييرات
- البند ١
- البند ٢

## مرتبط بـ
Closes #42

## قائمة التحقق
- [ ] الكود تم اختباره
- [ ] التوثيق محدّث
```

**مهم:** `Closes #42` يُغلق الـ issue تلقائياً عند الدمج!

---

## ٥️⃣ مراجعة ودمج

1. المراجع يفحص الكود
2. عند الموافقة: اضغط **Merge**
3. ✅ الـ Issue يُغلق تلقائياً!

---

## 🔗 الكلمات السحرية

هذه الكلمات في commit أو MR تُغلق الـ issues تلقائياً:

| الكلمة | التأثير |
|--------|---------|
| `Closes #42` | يُغلق الـ issue عند الدمج |
| `Fixes #42` | يُغلق الـ issue عند الدمج |
| `Resolves #42` | يُغلق الـ issue عند الدمج |
| `#42` | ربط فقط |

---

## 🚫 الممنوعات

1. ❌ Push مباشر على `main`
2. ❌ Commit بدون إشارة للـ issue (`#42`)
3. ❌ Commit كلمات سر أو أسرار
4. ❌ MR ضخم بـ 50 ملف
5. ❌ MR بدون `Closes #X`

---

## ✅ قائمة التحقق قبل الدمج

- [ ] اسم الـ branch يحتوي رقم الـ issue
- [ ] الـ Commits تتبع Conventional Commits
- [ ] الـ Commits تشير للـ issue (`#42`)
- [ ] وصف MR يحتوي `Closes #42`
- [ ] CI pipeline أخضر
- [ ] Label الـ issue مضبوط على `workflow::review`

---

## 🔗 روابط سريعة

| المورد | الرابط |
|--------|--------|
| لوحة المهام | `/-/boards` |
| جميع الـ Issues | `/-/issues` |
| Merge Requests | `/-/merge_requests` |
| العرض التفاعلي | `docs/guides/contributing/workflow-slides-ar.html` |

---

*هل لديك سؤال؟ أنشئ issue بالوسم `help-wanted`*

</div>
