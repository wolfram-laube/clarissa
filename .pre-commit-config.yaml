repos:
  - repo: local
    hooks:
      - id: orsa-snapshot-tests
        name: ORSA CLI snapshot tests
        entry: bash -lc "pytest -q tests/golden/test_cli_help_snapshot.py tests/golden/test_cli_demo_snapshot.py"
        language: system
        pass_filenames: false

      - id: orsa-unit-tests-smoke
        name: ORSA unit smoke (fast)
        entry: bash -lc "pytest -q tests/test_core_units.py"
        language: system
        pass_filenames: false
